/**
 * BookingCalendar — day/week court overview
 *
 * Features:
 *   - Colour-coded by booking type (regular/lesson/corporate/tournament/blackout)
 *   - Click slot → create booking on behalf of player
 *   - Click existing booking → edit/cancel
 *   - Staff can drag bookings to reschedule
 *   - Real-time updates via SSE or polling
 *
 * API: GET /api/v1/bookings/calendar?club_id=&view=week&date=
 */
export function BookingCalendar() {}
