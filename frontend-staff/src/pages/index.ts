// Staff Portal Pages (Next.js / React)
// /dashboard         → FinanceDashboard + CourtOverview (real-time)
// /calendar          → BookingCalendar (day/week view)
// /courts            → CourtScheduler (manage courts + blackouts)
// /bookings          → BookingSearch + BookingForm (create/edit/cancel)
// /bookings/[id]     → BookingDetail
// /players           → PlayerList + PlayerSearch
// /players/[id]      → PlayerProfile (skill history, wallet, suspend)
// /staff             → StaffList + TrainerSchedules
// /equipment         → EquipmentInventory + DamageReport
// /finance           → FinanceDashboard + TransactionLog + StripePayouts
// /reports           → UtilisationReport + RetentionReport + ExportButton
// /support           → TicketQueue + TicketDetail
// /settings          → ClubSettings + OperatingHours + PricingRules + StripeConnect
