// Player Web App Pages (Next.js)
// /                  → CourtSearch + OpenGamesFeed
// /search            → AvailabilitySearch (date/time/surface)
// /book/:courtId     → BookingFlow (4-step wizard)
// /open-games        → OpenGamesList (browse + join)
// /my-bookings       → UpcomingBookings + MatchHistory + WaitlistItems
// /profile           → ProfileSettings (name, password)
// /payments          → WalletPanel + SavedCards + InvoiceList
// /support           → SupportTicketForm + TicketHistory
