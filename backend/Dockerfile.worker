FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8080
# Override CMD per worker at Cloud Run deployment time
# e.g. CMD ["uvicorn", "app.workers.booking_worker:app", "--host", "0.0.0.0", "--port", "8080"]
