FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8080

# Default to booking worker — overridden per service at deploy time
CMD ["uvicorn", "app.workers.booking_worker:app", "--host", "0.0.0.0", "--port", "8080"]